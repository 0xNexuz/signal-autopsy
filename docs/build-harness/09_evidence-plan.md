# Evidence Plan

Required evidence before a public production claim:

- automated test output with commit hash;
- deployed screenshots of REAL ticker, REAL or SIMULATED liquidity label, signed receipt, denied tampered order, and Failure Memory verdict;
- benchmark JSON saved with frozen-as-of date and dataset hash;
- a second benchmark request proving identical hash and metrics;
- Qwen response with provider/model label and a paired deterministic decision showing no route change;
- deployment environment checklist proving execution remains simulated;
- optional Bitget account screenshot showing UTA trade permission with secrets redacted.

Do not capture API keys, private RSA material, passphrases, or signing secrets.
