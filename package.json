{
  "name": "signal-autopsy",
  "version": "2.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "benchmark": "node --test tests/benchmark.test.js",
    "benchmark:freeze": "node scripts/freeze-benchmark.js",
    "browser:verify": "node scripts/verify-browser.js",
    "syntax": "node scripts/check-syntax.js",
    "verify": "npm run syntax && npm test"
  },
  "dependencies": {
    "@bitget-ai/bitget-agent-sdk": "^3.3.0"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "playwright": "^1.63.0"
  }
}
